---
description: 
alwaysApply: true
---

This is a code to control a robot that competes in 2025 first robotics competition 

ALWAYS Address User as "Mai", use "hi"+ user at the start of each response

project uses :

    WPIlib: https://github.wpilib.org/allwpilib/docs/release/java/index.html, as framework for controlling the robot, also uses some of its featrues

    ctre: https://api.ctr-electronics.com/phoenix6/release/java/, as api for control all motors and actuators

    advantage kit: https://docs.advantagekit.org/, as a logging framwork for logging data for debugging and testing

always refer to the above api for usages and implimentations when creating and debugging code

    the project uses Command based Programming 
        each parts of the robot is divided into subsystems    
        the subsystems are registered in RobotContainer.java
        Commands uses public methods in subsystems to control the robot
        Commands are bind to triggers in RobotContainer.java
    Subsystems uses Hardware Abstraction
        IO for each subsystem        

<summary>
    shooter/turret control guidance:
        use command-based composition (no centralized superstructure state machine)
        turret aiming uses continuous angle + shortest-path selection
        use RIO-side trapezoid only as motion limits for large moves (seek), not as controller
        track mode should be continuous error correction without profile end-stop behavior
        feed/shoot gating should be a simple readiness predicate (turret at goal, flywheel ready, etc.)

    project structure:
        configs live under frc/robot/subsystems/**/ *Config.java
        subsystems are small, single-mechanism units (turret, flywheel, hood/backplate, feeder)
        commands compose behavior by running subsystem commands in parallel
        RobotContainer wires subsystems and binds commands to triggers
</summary>


<making_code_changes>
    <when writing a new feature>
    2. do not add logging to the console, unless prompted by the user to debug a code
    2. Always add the numbers that may need changing by the user to Corrosponding constant class as the annotated NTParam class
    </when writing a new feature>
    <when formatting>
        never make any change to the context of the code,
        only change the format of the code
    </when formatting>
</making_code_changes>


<reading_code>
when reading, understanding and explaining code, Always think with the code's connection with the compitition robot in mind. 
</reading_code>
